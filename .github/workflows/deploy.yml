name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Allow the workflow to publish to GitHub Pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Only one concurrent Pages deployment; don't cancel an in-progress one.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build single-file HTML
        run: npm run build

      - name: Assemble Pages site
        # Publish the whole repo (keeps index.html and the other questionnaires
        # working), then overlay the built single-file versions so the live
        # Attachment Reflection pages are the self-contained builds.
        run: |
          mkdir -p _site
          # Copy the site files (the questionnaires, diagrams, shared assets and
          # index), excluding tooling, docs, VCS and build artifacts.
          rsync -a --delete \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '.githooks' \
            --exclude 'node_modules' \
            --exclude 'dist' \
            --exclude '_site' \
            --exclude 'build' \
            --exclude 'docs' \
            --exclude 'package.json' \
            --exclude 'package-lock.json' \
            --exclude 'install-hooks.sh' \
            ./ _site/
          # Overlay the inlined single-file builds over the split reflection pages.
          cp dist/attachment-reflection.html         _site/reflection/attachment-reflection.html
          cp dist/attachment-reflection-results.html _site/reflection/attachment-reflection-results.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
