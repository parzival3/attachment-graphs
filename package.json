{
  "name": "attachment-graphs",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted, API-free questionnaire tools for piatorp.dk",
  "scripts": {
    "generate": "node build/generate-config.js",
    "build": "node build/build.js"
  },
  "devDependencies": {
    "inline-source": "^8.0.3"
  }
}
